
name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs_on: ubuntu-latest
    steps:
      - name: step1
        run: echo "Hello world"
      - name: step2
        run: echo "done bye"
  
  
